using AgroAnnuaire.Data;
using AgroAnnuaire.Models;

using AgroAnnuaireContext context = new AgroAnnuaireContext();

//var Lille = context.Sites
//                        .Where(p => p.Id > 2)
//                        .FirstOrDefault();

//if (Lille is Site)
//{
//    context.Remove(Lille);
//}

//context.SaveChanges();

//var Commercial = context.Services
//                        .Where(p => p.Id > 2)
//                        .FirstOrDefault();

//if (Commercial is Service)
//{
//    context.Remove(Commercial);
//}

//context.SaveChanges();

